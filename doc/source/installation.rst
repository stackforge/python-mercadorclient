============
Installation
============

At the command line::

    $ pip install mercador-client

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mercador-client
    $ pip install mercador-client
